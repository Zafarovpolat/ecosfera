# Нормализация переводов строк: в репозитории всегда LF
* text=auto

# Ассеты — бинарные, git не должен пытаться их сливать или менять переводы строк
*.webp binary
*.woff2 binary
*.ico binary
*.png binary
*.jpg binary
